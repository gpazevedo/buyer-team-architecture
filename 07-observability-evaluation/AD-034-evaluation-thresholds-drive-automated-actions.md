# AD-034 — Evaluation Score Thresholds Drive Automated Actions (Closed Loop)

**Theme:** Observability & Evaluation  **Catalog:** AD-34 · **Source PRD:** PRD-004 · **Status:** Accepted · **Related:** AD-30, AD-32, AD-33, AD-35, AD-133, AD-43

## Context

Detecting quality drift is only half the problem; the system must act on it without waiting for a human in the common cases. Without a binding consequence for each evaluator's score, quality drift degrades silently. Compliance-critical violations (confidentiality, winner disclosure, governance) require immediate escalation rather than deferred review. During memory degradation, the baseline quality threshold must relax to avoid false alarms triggered by reduced evaluation completeness rather than genuine model regression.

## Decision

Bind each evaluator to a minimum score, a CloudWatch alarm threshold, and an automated action wired through CloudWatch alarms → SNS:

- Negotiation Quality Composite < 0.60 → model rollback.
- Communication Tone < 0.80 → disable auto-send.
- Rationale Defensibility < 0.70 → block auto-award.
- Kraljic Accuracy < 0.90 → invalidate semantic cache and alert.
- Bid Format / Governance Compliance < 1.00 → alert immediately / halt new negotiations.

Confidentiality and winner-disclosure violations escalate to Compliance on a single event. During memory degradation, the quality alarm threshold relaxes to 0.85 (PRD-004 §2.3.2 / REQ-O209) to avoid false alarms caused by reduced evaluation completeness rather than genuine model regression.

## Alternatives Considered

- **Thresholds drive alerts only, no automated actions.** Rejected: alert-only requires a human to react to every quality drift event; this latency is unacceptable for events that block negotiations or require model rollback.
- **Single global quality threshold with no per-evaluator binding.** Rejected: different evaluators measure different failure modes with different consequences; a single threshold cannot distinguish a tone regression (disable auto-send) from a governance violation (halt + compliance escalation).
- **Status quo / no action.** Rejected: without binding consequences, quality drift degrades silently until a human notices.

## Trade-offs

| Gained | Given up |
| --- | --- |
| Quality control is closed-loop and largely hands-off for routine drift; the system can roll back models, disable auto-send, block auto-award, and halt new work without manual intervention | A flaky or noisy evaluator could trigger automated rollback or halt, over-reacting to a transient signal |
| Compliance-critical violations (governance, confidentiality, winner disclosure) still escalate immediately to humans regardless of automation | Online evaluation alarms run over multi-minute periods (e.g., 15 min), so some sub-threshold output can ship before the automated action fires |
| Degraded-mode threshold relaxation prevents false alarms during memory degradation | Governance and format compliance alarm at exactly 1.0 — any single violation halts or alerts, which may interrupt throughput for a genuine edge case |

The LLM-as-Judge consistency requirement (±0.1 across three runs) mitigates the false-positive risk from flaky evaluators. The bounded detection lag is an explicitly accepted trade-off in favor of managed-service simplicity over a custom real-time pipeline.

## Results

Routine quality drift is handled automatically without human intervention. Compliance-critical violations escalate immediately to humans. The accepted liabilities are eval-driven false positives and a bounded detection lag of up to one alarm period. AD-30 (Transaction Search) is the prerequisite for evaluators to have input; AD-32 defines the evaluator types; AD-33 defines the sampling strategy that determines which traces contribute scores. AD-35 extends the closed loop with scheduled ATLAS-specific evaluators whose failures also gate deployment.

**Not built (as of PR #108, 2026-07-02).** None of the five CloudWatch-alarm-driven automated actions described above (model rollback, disable auto-send, block auto-award, invalidate semantic cache, halt on governance/format violation) have been observed wired to CloudWatch alarms or SNS. This closed loop is only as real as the evaluator scores feeding it, and per AD-32/AD-33's Results only the Kraljic Accuracy ground-truth check is implemented and CI-gated today — offline, not against live alarms. REQ-O223 (threshold re-baselining trigger) is implemented as a negotiation-count countdown persisted in `{env}-system-config`, published via `resilience.metrics.emit_metric` rather than OTEL (the orchestrator Lambdas' ADOT layer is trace-only — see [AD-031](AD-031-w3c-traceparent-propagation.md)'s Results for why that layer was dark until the same day). This ADR's decision stands as the target design; treat every action in the Decision section as unimplemented until an individual Results update says otherwise.

**Update 2026-07-04: the detection/alerting half is now built — CloudWatch alarms exist, but no automated action fires from them yet.** A new SNS topic (`aws_sns_topic.evaluation_alerts`, `infra/modules/messaging/sns.tf`) is the `alarm_actions` target for 4 new CloudWatch alarms in `infra/modules/step-functions/alarms.tf`: Bid Format Compliance < 1.00, Governance Policy Compliance < 1.00, Communication Tone < 0.80, and Negotiation Quality Composite < 0.60 — the exact thresholds this ADR's Decision names, alarming directly on the live `procurement/evaluation` metric/dimension pairs `node_bid_evaluation.py` and `node_award_comms.py` already emit. **Only 4 of the 5 evaluators could be wired**: Rationale Defensibility (`evals/llm_judge.py`) and Kraljic Accuracy (`evals/dataset_coverage.py`) remain CI-only/offline per AD-32's Results — no live metric exists in CloudWatch for either, so no alarm exists yet for the "Rationale Defensibility < 0.70" or "Kraljic Accuracy < 0.90" thresholds. The SNS topic has **no subscriber and no Lambda consumer** — this is alert-only. None of the five automated actions in the Decision section (model rollback, disable auto-send, block auto-award, invalidate semantic cache, halt new negotiations) exist; a human still has to notice the alarm state and act on it manually. Confidentiality/winner-disclosure escalation-to-Compliance and the memory-degradation 0.85 threshold relaxation also remain unbuilt.

**Correction 2026-07-13: one of the "4 wired" alarms has been dead since 2 days after this update.** The `eval_negotiation_quality` alarm (Negotiation Quality Composite < 0.60) was provisioned against the live `procurement/evaluation` / `negotiation_quality` metric pair that `node_award_comms.py` emitted at the time. PR #151 (2026-07-06) removed the `bid_evaluation_llm` agent and, with it, the only call site that ever emitted that metric (see AD-32's 2026-07-06 correction note) — nothing was updated on the alarm side to match. `treat_missing_data = "notBreaching"` means this alarm has sat in OK/insufficient-data state ever since rather than surfacing as broken; it will never fire again until something re-emits `negotiation_quality` to that namespace/dimension. Discovered via a docs-review pass, not a live incident — no evidence anyone was relying on this alarm having tripped. The other 3 wired alarms (Bid Format Compliance, Governance Policy Compliance, Communication Tone) are unaffected; their metrics are still live.

**Update 2026-08-01 (impl PR #252): the detection half is now gone too — all 4 alarms deleted.** `infra/modules/step-functions/eval_alarms.tf` was removed in a dev CloudWatch cost trim, taking Bid Format Compliance, Governance Policy Compliance, Communication Tone, and the already-dead Negotiation Quality alarm with it. The reasoning is the 2026-07-04 update's own admission carried to its conclusion: the `evaluation_alerts` SNS topic has **zero subscriptions**, and none of the five automated responses in this ADR's Decision section were ever wired — so these were alert-only signals that notified nobody, at $0.10/alarm-month each. CloudWatch bills per alarm-month regardless of state and regardless of whether actions are enabled, so deleting the alarms was the only lever that reduced the bill. **The `procurement/evaluation` metrics themselves are untouched** — `node_bid_evaluation.py` and `node_award_comms.py` still emit them, and they are still charted on the application dashboard; what is gone is the threshold binding, which is what this ADR is actually about. The rationale is tombstoned in the sibling `infra/modules/step-functions/alarms.tf` so it stays discoverable at the site. Re-add together with a subscriber and at least one wired action — an alarm on an unsubscribed topic is a chart with a billing line. See [AD-133](../08-cost-architecture-optimization/AD-133-delete-and-replay-dev-alarms-across-pause.md) for the cost decision this falls out of. As of this update **nothing in this ADR's closed loop is live**: no automated action, and now no detection either.

**Update 2026-08-05 (impl PR #259):** the subscriber half of the re-add condition above is now met — `evaluation_alerts` has its first real subscriber, applied to dev and confirmed live (see AD-133's 2026-08-05 update for the mechanism and AD-43's for the apply details). The alarms themselves remain deleted and no automated action exists, so this ADR's closed loop is still not live; a subscriber alone doesn't re-add a threshold binding, only removes one reason not to.

**Update 2026-08-12: all 4 detection alarms re-added; the confidentiality/winner-disclosure escalation half of this ADR is now genuinely live, on its own severity tier.** Two changes:

1. **Detection re-added for all 4 evaluators.** `eval_bid_format_compliance`, `eval_governance_policy_compliance`, `eval_communication_tone`, and `eval_negotiation_quality` are back (`infra/modules/step-functions/eval_alarms.tf`), wired to `evaluation_alerts` (real subscriber since PR #259). `negotiation_quality` was restored last, alongside re-wiring its dead metric — `node_award_comms.py` now scores `negotiations.reasoning` (persisted at Node 3/4x) post-award, re-purposing the bid-evaluation-rationale rubric onto the strategy agents' reasoning, the only rationale text now available on a negotiation.
2. **The confidentiality/winner-disclosure bullet is no longer "unbuilt."** Re-reading this ADR's own Decision text: unlike the other four bullets (model rollback, disable auto-send, block auto-award, invalidate cache — all real system-behavior changes, still genuinely unbuilt), "confidentiality and winner-disclosure violations escalate to Compliance on a single event" is *only* an escalation/notification requirement. Two controls already existed independently of this ADR and were never credited here: (a) `WinnerDisclosureGuard` (`agents/award_comms_llm/steering.py`) — a pre-call structural guard that cancels any tool call carrying a winner-identifying field, making disclosure impossible by construction, not just detected after the fact; (b) `governance_confidentiality_violation` (`infra/bucket_a_alarms.tf`, added earlier — see `PRDs/observability-backlog.md`'s Bucket A pass) — a real, already-applied CloudWatch alarm on `governance.violation_count{violation_type=confidentiality_leak}`, the metric all three confidentiality/winner-disclosure guards (`WinnerDisclosureGuard`, `BidConfidentialityGuard`, `AuctionIntegrityGuard`) emit when they fire. What was missing was only the "to Compliance" part — it alarmed onto the same undifferentiated `evaluation_alerts` topic as routine platform noise. Closed today: a new `alerts_critical` SNS topic (`infra/modules/messaging/sns.tf`), the severity split `PRDs/observability-backlog.md`'s Question 2 proposed 2026-07-05 and explicitly deferred pending an actual subscriber (now true, PR #259) — `governance_confidentiality_violation`'s `alarm_actions` now points there instead of `evaluation_alerts`. The topic-rename that note also proposed (`evaluation_alerts` → `alerts_warning`) was skipped: `aws_sns_topic.name` is `ForceNew`, so renaming would destroy/recreate the topic and force the PR #259 subscriber to reconfirm; `evaluation_alerts` keeps its name and stands in as the warning tier.

**Apply status (updated same day):** `eval_bid_format_compliance`, `eval_governance_policy_compliance`, `eval_communication_tone`, the `alerts_critical` topic, and the `governance_confidentiality_violation` repoint are merged (impl PRs #266/#267) and applied to dev. `eval_negotiation_quality` (impl PR #269) re-wires the dead metric and is still pending merge/apply. The `alerts_critical` email subscription is not yet wired: `alert_email` lives in a gitignored local `terraform.tfvars`, so `dev-deploy.yml` runs with it empty and does not create the subscription (the same mechanism that left `evaluation_alerts`'s subscriber a manual `PendingConfirmation` step on 2026-08-05) — the critical topic needs a one-time manual subscribe + confirmation before it actually pages anyone.

**Still fully unbuilt, unchanged by today's update:** the other four AD-034 automated actions (model rollback, disable auto-send, block auto-award, invalidate semantic cache, halt new negotiations) remain alert-a-human-only, same as every prior update recorded. Today's update closes the one bullet in this ADR's Decision that was ever *only* an escalation requirement, not a behavior change — it does not narrow that remaining gap.

**Update 2026-08-13 (impl PR #269): `eval_negotiation_quality` is merged and applied — all 4 detection alarms from the 2026-08-12 update are now live simultaneously for the first time since the 2026-07-13 correction first found the metric dead.** The re-wire (`node_award_comms.py` scoring `negotiations.reasoning` post-award, per the 2026-08-12 update's description) is deployed to dev alongside the alarm. Detection is now fully re-established: `eval_bid_format_compliance`, `eval_governance_policy_compliance`, `eval_communication_tone`, and `eval_negotiation_quality` all alarm on live metrics, all wired to `evaluation_alerts` (subscribed since PR #259). This closed-loop's automated-action half is unaffected by today's update — see the paragraph above, still unbuilt.

**Update 2026-08-15 (impl PR #300, #294, #299): the first of the five automated actions is built — Communication Tone < 0.80 now disables auto-send for real.** `infra/ad034_remediation.tf` (root-level, same placement as `guardduty.tf`) adds a remediation Lambda subscribed to `evaluation_alerts`: it filters to exactly one alarm (`{env}-buyer-team-eval-communication-tone`, the alarm `eval_alarms.tf` already provisions) and only its `ALARM` transition, then flips `governance/default.auto_send_communications = False` via `update_item` scoped to `dynamodb:LeadingKeys=["governance"]` (the policy condition's syntax was corrected by direct commit c7a8cf7 — `ForAllValues:StringEquals`; the earlier nested form would not have parsed as written). There is deliberately no auto-re-enable: a human flips the flag back via `scripts/toggle_auto_send.py` (which touches only the `auto_send_communications` attribute), matching `eval_alarms.tf`'s "a human has to notice and act" philosophy this ADR's own notes have cited. Two timing facts bound the action: (a) Node 7 runs `_run_communications()` synchronously *before* the async Communication Tone judge self-invokes, so the flip can only protect the *next* message, never the one that tripped the alarm; (b) PR #299 bounds `_auto_send_enabled()`'s cache to a 30s TTL (previously container-lifetime) so the flip takes effect on already-warm execution environments within ~30s rather than never.

PR #294 adds a new detection binding adjacent to the five this ADR's Decision names: `kraljic_online_eval_helpfulness < 0.85` — a Metrics Insights alarm on AgentCore's sampled Phase-B online-eval signal (AD-32), wired to `evaluation_alerts`. Alert-only, no automated action; `treat_missing_data = "notBreaching"` until sampled traffic confirms the metric name (the namespace was still empty in dev when the alarm shipped). The other four automated actions (model rollback, block auto-award, invalidate semantic cache, halt new negotiations) remain unbuilt. Apply note: all of the above merged 2026-08-15; the root-level Terraform is picked up by `dev-deploy.yml`'s untargeted apply on the merge push — live dev verification was not part of this review.

**Update 2026-08-15 (P1): the second of the five automated actions is built — Bid Format / Governance Policy Compliance < 1.00 now halts new negotiations for real.** `infra/ad034_halt_negotiations_remediation.tf` (root-level, same pattern as `ad034_remediation.tf`) adds a remediation Lambda subscribed to `evaluation_alerts`, filtered to the two alarm names `eval_bid_format_compliance` and `eval_governance_policy_compliance` bind to per this ADR's Decision text (one Lambda, two alarm names, not two near-identical Lambdas) and only their `ALARM` transitions. On fire it flips `governance/default.negotiations_halted = True` via `update_item` scoped to `dynamodb:LeadingKeys=["governance"]` — the same IAM condition shape as the Communication Tone action. `orchestrator/node_ingest_validate.py` (Node 1, the graph entry point) reads the flag directly with a 30s TTL cache (mirroring `supplier_comms.delivery._auto_send_enabled`'s idiom) and returns a CANCELLED terminal response instead of creating a negotiation while it's set; the source PR is left at VALIDATED (untouched) so it resumes automatically once a human clears the flag via `scripts/toggle_negotiations_halt.py --resume`. Existing negotiations already past Node 1 are unaffected — this halts new work only, matching the Decision text's wording. Model rollback and block auto-award and invalidate semantic cache remain unbuilt; unlike this action, their underlying evaluators (Rationale Defensibility, Kraljic Accuracy) still have no live CloudWatch alarm to trigger from (CI-only/offline per AD-32's Results), so building their remediation Lambdas next would leave them subscribed to nothing.

**Update 2026-08-15 (P2, impl PR #308): the third of the five automated actions is built — Negotiation Quality Composite < 0.60 now rolls the model tier map back to the previous mapping.** `infra/ad034_model_rollback_remediation.tf` (root-level, same pattern as its two siblings) adds a remediation Lambda subscribed to `evaluation_alerts`, filtered to exactly the `eval_negotiation_quality` alarm and only its `ALARM` transition. On fire it restores `model/previous.tiers` into `model/default.config_json.tiers` (only `tiers` is replaced — every other blob field is preserved) and bumps the row's top-level `model_epoch`, scoped to `dynamodb:LeadingKeys=["model"]`. Two pieces of surrounding machinery make the rollback possible at all: (a) **the snapshot** — nothing wrote `model/default` except seeding before this PR, so there was no previous-mapping row to restore; `scripts/update_model_tier.py` (new) is now the human model-upgrade path, and every `--tier X --model Y` bump snapshots the old tiers map into `model/previous` first (its `--restore-previous` flag is the manual form of the same rollback); (b) **warm-container propagation** — agents resolve their model once at container boot, so `buyer_agent_core`'s `AgentBlueprint` now re-checks `model_epoch` on a 30s TTL (first-read baseline, thread-safe, and never rebuilding on a failed read or an env/seed resolution fallback), the same warm-process contract PR #299 gave the auto-send flag: the flip takes effect on already-warm execution environments within ~30s instead of never. There is deliberately no auto-re-apply — a human must move the tier map forward again via the script — and with no `model/previous` row (nothing was ever bumped) or identical maps, the handler logs and does nothing. **Remaining: 2** — block auto-award and invalidate semantic cache, both still alarm-less per the P1 paragraph's note.

**Update 2026-08-15 (P3, impl PRs #309, #311, #314, #316): the fourth and fifth automated actions are built — Rationale Defensibility < 0.70 blocks auto-award, and Kraljic Accuracy < 0.90 invalidates the semantic cache. All five actions are now built, and the P1 paragraph's "no live alarm to trigger from" gap is closed.** Both follow the established pattern (detection alarm on a live emitter, then a remediation Lambda subscribed to `evaluation_alerts` filtered to exactly that alarm's ALARM transition), and both needed the detection half first for precisely the alarm-less reason P1 recorded. Detection: `node_award_comms.py` now scores `negotiations.reasoning` with a Rationale Defensibility judge on the async B2 path (one Haiku call per award, ~$0.001) and emits `rationale_defensibility`; `eval_alarms.tf` gains `eval_rationale_defensibility` (Average, < 0.70, impl PR #309). Kraljic Accuracy — which has no online reference-inputs source, so Builtin.Correctness cannot run on the sampled path — is scored by a daily production-faithful `with_axes` harness run (`eval-kraljic-accuracy-daily.yml`, impl PR #311) that `put-metric-data`s `kraljic_accuracy`; `eval_alarms.tf` gains `eval_kraljic_accuracy` (Minimum, < 0.90). The daily cadence deserves its own note against the alarm's 900s period: the alarm evaluates one fresh datapoint per day, and the workflow deliberately emits NOTHING on infra-down skip days (exit 3) so `treat_missing_data = "notBreaching"` keeps it inert — a bogus 0.0 there would spuriously invalidate the cache. Action 4 (impl PR #314): on ALARM the Lambda flips `governance/default.auto_award_blocked = True` (same `dynamodb:LeadingKeys=["governance"]` IAM shape as its siblings), `node_approval_gate.py` reads the flag directly (30s TTL) and returns PENDING_APPROVAL with `block_reason = "auto_award_blocked"` instead of auto-awarding, and there is no auto-re-enable — `scripts/toggle_auto_award_block.py --resume` is the human path. Live-verified 2026-08-15 via a synthetic ALARM SNS event against the deployed dev Lambda: wrong-alarm and OK-transition payloads ignored, ALARM transition flipped the flag, restore clean. Action 5 (impl PR #316): on ALARM the Lambda executes `SET cache_epoch = if_not_exists(cache_epoch, :zero) + :one` — an epoch, not a scan+delete: atomic, no scan RCU, no delete-repopulation race, the direct precedent being `model_epoch` — and `node_kraljic_classify.py` threads the epoch into the cache-key hash, so old rows become unreachable and expire via the existing 24h TTL (absent attribute → epoch 0 → hashes unchanged today, backward compatible). Monotonic by construction, so no toggle script. **Remaining: 0** — all five of this ADR's automated actions are built.

**Update 2026-08-16 (impl PR #315): the Kraljic Accuracy signal gains a PR-gate sibling (Phase 6 of the hardening plan).** `pr-checks.yml`'s new `kraljic-accuracy-gate` job invokes the live dev Kraljic Classifier through the production path (`orchestrator/agent_invoke.py`) in `name_only` mode and fails the PR below a 0.80 floor — deliberately below the measured 17/20 = 85% baseline (2026-08-13). Exit 3 (>= 80% of rows unclassified, or zero rows — the dev VPC cost-pause surfaces as 424s) is SKIPPED with a warning and never emits metrics: a paused dev VPC must not block merges, and a bogus 0.0 would spuriously trip the cache-invalidation alarm. This is a deployment gate, not a detection binding — no alarm, no automated action.

---
*Part of the [Buyer Team architecture](https://buyer-team.com) decision record · by [Gustavo Peixoto de Azevedo](https://linkedin.com/in/gpazevedo)*
