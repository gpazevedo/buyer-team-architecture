# AD-029 — Four-Layer Observability (Platform / Application / Domain / FinOps)

**Theme:** Observability & Evaluation  **Catalog:** AD-29 · **Source PRD:** PRD-004 · **Status:** Accepted · **Related:** AD-30, AD-31, AD-13, AD-61, AD-115, AD-120, AD-121, AD-126

## Context

Agentic systems resist conventional monitoring for four reasons: non-determinism, multi-hop reasoning, cost opacity, and silent quality drift. A single undifferentiated stream of metrics cannot serve the four audiences who care about the system — SRE, procurement, and FinOps — because the questions they ask are at different altitudes. Runtime health ("is the agent erroring?"), business outcome ("did we save money on this negotiation?"), and unit economics ("what is this costing us to run?") must not be coupled to the same emission path. Without separation, new domain metrics would couple to runtime internals and per-audience dashboards would become impossible to maintain without breaking the others.

## Decision

Observability is split into four independent layers, each with its own audience, ownership, and CloudWatch namespace(s):

- **Layer 1 — Platform:** AgentCore built-in metrics (session count, invocation latency, error count/rate, throttle count, token usage) under `AWS/Bedrock-AgentCore`, plus `AWS/Lambda`/`AWS/States` infrastructure metrics, emitted with no code instrumentation required. Audience: SRE.
- **Layer 2 — Application:** ADOT/OTEL spans carrying procurement-specific and security attributes (`procurement.tenant_id`, `procurement.negotiation_id`, `security.steering.*`, etc.) plus the `procurement/resilience` metrics, via custom instrumentation. Audience: SRE/engineering.
- **Layer 3 — Domain:** Custom OTEL business metrics published as EMF to `procurement/business`, `procurement/kpi`, `procurement/resilience`, `procurement/security`, and `procurement/tenant`. Audience: procurement.
- **Layer 4 — FinOps:** Cost and unit-economics metrics under `procurement/cost` — app-emitted token/estimated-cost metrics (`tokens.*`, `token.estimated_cost_usd`, `negotiation.total_cost_usd`, `agentcore.session_seconds`, `cache.hit`) **and** the republished AWS-billed actuals (`aws_cost.usd` from the Cost Explorer poller, AD-126). Audience: FinOps.

**Axis note.** Layers 1–3 were originally distinguished by *instrumentation mechanism* (free AWS-native metrics / OTEL spans / app-emitted EMF). Layer 4's `procurement/cost` EMF is mechanically identical to Layer 3's Domain EMF — it is separated on the *question it answers* (unit economics, a distinct FinOps altitude) rather than on how it is emitted. This re-bases the taxonomy toward audience/altitude, consistent with the dashboard re-split recorded in the 2026-07-09 update below. `aws_cost.usd` (AD-126) reinforces the split independently: as a republished billing feed it is neither AWS-native runtime metric (L1), OTEL span (L2), nor app business EMF (L3), and fits no layer but this one.

## Alternatives Considered

- **Single unified observability layer.** Rejected: it would either lack business context (tenant, savings, Kraljic quadrant) or couple business KPIs directly to runtime internals, making per-audience dashboards unworkable.
- **Two layers (Platform + combined Application/Domain).** Rejected: merging application traces with domain EMF metrics conflates operational debugging with business KPI reporting, preventing independent evolution of either.
- **Three layers, with cost folded into Domain.** The original form of this decision. Rejected in favour of a dedicated FinOps layer once cost outgrew Domain: `procurement/cost` acquired its own emitter, dashboard, `finops-alerts` routing, owning PRD (PRD-009), and ADRs (AD-61, AD-126), and the token metrics kept oscillating between the Domain and Application dashboards because they answered a FinOps question, not a business-KPI one (they were moved off the Business dashboard to a dedicated FinOps dashboard on 2026-07-09 for exactly this reason). Folding cost into Domain left it homeless; promoting it to Layer 4 gives it a principled home.

## Trade-offs

| Gained | Given up |
| --- | --- |
| Clean per-audience dashboards (Platform, Application, Domain, FinOps) that can each evolve independently | A single negotiation now spans all four layers, requiring a correlation mechanism (AD-31) to recombine |
| Layer 1 costs nothing to emit; no code instrumentation for platform metrics | Layers 2–4 require explicit instrumentation and maintained contracts per metric |
| New domain metrics can be added purely at Layer 3, and new cost/unit-economics metrics purely at Layer 4, without disturbing the others | Cost metrics with high-cardinality dimensions (e.g., `negotiation_id`, `model_id`) are a known CloudWatch cost risk flagged as an open follow-up in PRD-004/PRD-009 |
| Layer 4 separates unit economics (FinOps altitude) from business outcome (procurement altitude), which the earlier three-layer form conflated | Layer 4 breaks the original "one layer = one instrumentation mechanism" rule: its EMF is mechanically identical to Domain's, separated on audience/altitude instead (see Axis note) |

## Results

Four role-aligned dashboards fall out naturally: Platform (SRE), Application (engineering), Domain (procurement), FinOps. Each layer can change without disturbing the others; new domain metrics were added over successive PRD-004 revisions purely at Layer 3, and cost metrics purely at Layer 4. AD-31 (W3C `traceparent` propagation) provides the cross-layer correlation substrate; AD-30 (CloudWatch Transaction Search) is the prerequisite for evaluators to read Layer 2 traces. The accepted liability is CloudWatch cardinality and cost management, particularly for the unbounded-cardinality `negotiation_id` (Layer 3/4) and `model_id` (Layer 4) dimensions.

**Update 2026-07-04/05 — Layer 1 real namespace corrected; Layer 1 alarms and new Layer 3 metrics shipped.** The Layer 1 namespace is `AWS/Bedrock-AgentCore` (hyphenated) with real metric names `Errors` and `Latency` (also `SystemErrors`/`UserErrors`/`Duration`/`Sessions`/`Invocations`), confirmed against live dev CloudWatch data via `list_metrics` — not the unhyphenated `AWS/BedrockAgentCore` name this ADR originally stated and PRD-004 §3.3 guessed. `infra/agent_runtime_alarms.tf` (PR #139) adds 16 `aws_cloudwatch_metric_alarm` resources (Errors + Latency, `for_each` over the 7 agent runtimes + the skill runtime) on this corrected namespace, routed to the same `evaluation_alerts` SNS topic AD-34's alarms use — Layer 1 was previously "no code instrumentation required" but also **no alarms**; an agent up-but-erroring or slow was invisible until it caused a node Lambda failure. The Latency threshold (60s) is a first-pass guess, not a tuned SLO — there is no fixed per-invocation timeout for agent runtimes the way Lambda has one.

New Layer 3 domain metrics shipped in the same pass (PR #140): `tokens.input`/`tokens.output` under `procurement/cost` (dimensioned by `agent_name` + `model_tier`, emitted from `buyer_agent_core.observability.log_usage()`), `kraljic.classification_source` under `procurement/business` (one `emit_metric` call at the single `_response()` choke point all 5 Kraljic classification paths return through), and `governance.violation_count` under `procurement/business` (emitted from each of the 6 agents' `steering.py` guards at their block/intervention point, dimensioned by `violation_type`). A role-aligned Business dashboard (`infra/modules/observability/dashboard.tf`, PR #137/138) now visualizes negotiation lifecycle, bid/governance/approval, savings, and the new token-usage widgets — none of these three new metrics has an alarm yet; thresholds need real dev data first. `emit_metric` itself gained a self-observing failure mode (AD-115) as part of this same body of work.

**Update 2026-07-08 — Layer 3 emission corrected to true EMF; approval-gate trace-continuity gap closed.** The Layer 3 decision above always said "published as EMF," but until PR #164 (orchestrator `resilience/metrics.py`) and PR #166 (agent-side `buyer_agent_core/metrics.py`), `emit_metric` actually called a synchronous `boto3 put_metric_data` — a 5s-connect + 5s-read-timeout network call, retried once, sitting in the A2A hot path (`agent_invocation.py`'s `finally` block, every per-node emission, every LLM step's `log_usage`). Both PRs replaced it with a genuine EMF stdout log line, removing up to ~20s of blocking I/O per call in the failure case and real network latency even on the happy path — the implementation now matches what this ADR described from the start. This also retired AD-115's `put_metric_data`-failure fallback datapoint, since there is no longer a remote call for the primary path to fail (see AD-115's correction). Separately, PR #164 closed the last hole in AD-31's promised single coherent trace: the human-approval-gate pause (`node_approval_gate`'s `waitForTaskToken`) previously broke trace continuity on *every* task-token resume, not only human-reviewed ones, because `_send_success` shipped its output to Step Functions before the (then post-hoc) `inject_trace_context` call could run. See AD-31's update and the new **AD-120** for how `correlation_id` and the OTEL carrier now survive that pause as persisted row data rather than in-band propagation.

**Update 2026-07-09 — Dashboards re-split along the layer model; meta-observability heartbeat added to Platform.** The three role-aligned dashboards described in the original Results section (Operations/SRE, Business/procurement, Cost/FinOps) were replaced by four, realigned directly to the layer split: **Platform** (pure `AWS/*` infra health, Layer 1), **Application** (circuit breakers and eval-quality signal, plus 5 newly-added `procurement/resilience` widgets — compensation/saga outcomes, award-retraction/bid-withdrawal notices, DLQ redrive outcomes, recovery-lock health, and the two REQUIRES_ATTENTION escalation triggers — found via a full-repo audit of `emit_metric(` call sites that had never had a dashboard widget, Layer 2), **Domain** (renamed from Business; `procurement/business`/`procurement/kpi`, Layer 3), and **FinOps** (`procurement/cost`, pre-existing, now the sole home for the Token Usage widget dropped from Domain — the dashboard-level realization of what is now Layer 4). Platform also plots **AD-121**'s `pipeline_heartbeat` metric alongside the pure `AWS/*` widgets — a custom `procurement/observability` metric, but a platform-health question ("is the metrics pipeline itself alive") rather than a business one. *At the time this update was written, the dashboards had gone to four but this ADR's Decision still declared three layers; the 2026-07-20 update below closed that gap by formally promoting FinOps to Layer 4, so the four dashboards now map 1:1 to the four layers.*

**Update 2026-07-20 — FinOps promoted to a fourth layer (Decision, Context, Alternatives, Trade-offs, Results rewritten above).** The 2026-07-09 dashboard re-split had produced four audience dashboards against a three-layer Decision, and `procurement/cost` was filed under Layer 3 (Domain) while being dashboarded, alarmed (`finops-alerts`), and owned (PRD-009, AD-61, AD-126) as FinOps — with the token metrics repeatedly shuffled between the Application and Domain dashboards because the model gave them no home. This update formalizes what was already deployed: cost/unit-economics moves out of Layer 3 into a new **Layer 4 — FinOps**, and the layer taxonomy's defining axis is stated explicitly (audience/altitude, not instrumentation mechanism — see the Axis note under Decision). No metric, namespace, emitter, dashboard, or alarm changed — this is a taxonomy/doc reconciliation. Paired doc updates: PRD-004 §1/§2/§2.4/§3.1/REQ-O400, PRD-001 §index, `observability-monitoring-runbook.md` §1, `architectural-decisions-catalog.md` AD-29 row. File renamed `AD-029-three-layer-observability.md` → `AD-029-four-layer-observability.md` (only `adr/README.md` referenced the old path).

---
*Part of the [Buyer Team architecture](https://buyer-team.com) decision record · by [Gustavo Peixoto de Azevedo](https://linkedin.com/in/gpazevedo)*
